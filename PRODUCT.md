# Product

## Register

product

## Users

ZhiCore 的主要使用者是内容社区作者、维护者和需要发布技术内容的用户。他们在桌面端或较宽屏设备上完成文章草稿、结构化内容预览、保存和发布前检查，也需要在移动端查看和做轻量编辑。

## Product Purpose

ZhiCore 前端承载内容社区的写作、阅读、互动和管理体验。编辑器页面的目标是让用户像写普通文章一样连续写作，同时把正文保存事实收敛到后端 `schemaVersion + blocks` 模型，并在发布前提供可信的读者预览。

## Brand Personality

克制、专注、可靠。界面语气应服务写作流程，减少装饰和管理后台感，让状态、结构和错误提示清楚但不打断正文节奏。

## Anti-references

不做厚重的 block 卡片编辑器，不把正文拆成 JSON 表单或配置面板，不用装饰性动效和高饱和渐变包装写作区。编辑器默认态不应像营销页、数据看板或工程文档阅读器。

## Design Principles

- 专注写作优先：标题、正文、保存状态和发布动作是主线，工具栏只在需要时提供辅助。
- 结构事实不外露成负担：后端 blocks 是保存契约，前端只在预览、校验、定位和必要的 gutter 上暴露结构。
- 所见可信：预览消费编译后的结构化模型，不直接渲染用户输入 HTML。
- 状态可解释：保存、字数、草稿指纹、预览同步和不支持结构都应给出明确反馈。
- 移动端保留任务流：窄屏优先切换写作与预览，不强行压缩左右双栏。

## Accessibility & Inclusion

默认按 WCAG AA 的可读性和焦点可见性推进；正文、按钮、辅助文本需要足够对比度。编辑器交互应支持键盘焦点、稳定控件尺寸、减少布局跳动，并尊重 `prefers-reduced-motion`。
